[project]
name = "llm-client"
version = "0.1.0"
description = "A desktop LLM client with Material Design UI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "customtkinter>=5.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
llm-client = "llm_client.main:main"
